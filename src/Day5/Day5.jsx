import ReactForm from "./ReactForm";

const Day5 = () => {
  return (
    <div className="container">
      <h2>
        Topic: Build a form (name, email) with validations (Controlled vs
        Uncontrolled input)
      </h2>
      <ReactForm />
    </div>
  );
};

export default Day5;
